<%= form_with(model: persona, local: true) do |form| %>
  <% if persona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(persona.errors.count, "error") %> prohibited this persona from being saved:</h2>

      <ul>
      <% persona.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :persona_nombre %>
  </div>

  <div class="field">
    <%= form.label :run %>
    <%= form.text_field :run, id: :persona_run %>
  </div>

  <div class="field">
    <%= form.label :confianza %>
    <%= form.number_field :confianza, id: :persona_confianza %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario, id: :persona_comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
